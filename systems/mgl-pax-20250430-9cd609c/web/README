Static files for web server in MGL-PAX/WEB and for
PAX:UPDATE-ASDF-SYSTEM-HTML-DOCS.

There are options:

- "default" has a CSS file that uses the default fonts (sans-serif,
  serif, monospace);

- "charter" has a CSS file that uses the XCharter and XEB Garamond
  fonts (bundled).
